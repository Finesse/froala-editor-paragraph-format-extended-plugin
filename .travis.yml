language: node_js
node_js:
    - node

install:
    - npm install

script:
    - npm run build

deploy:
    provider: npm
    email: finesserus@gmail.com
    api_key: $NPM_TOKEN
    on:
        tags: true

notifications:
    email: false
